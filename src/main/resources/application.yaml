server:
  port: 8080

spring:
  application:
    name: e-commerce-site-service
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce_service
    username: root
    password: Abhi@2109
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    defer-datasource-initialization: true
